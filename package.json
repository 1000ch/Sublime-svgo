{
  "version": "7.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "get-stdin": "^9.0.0",
    "svgo": "^3.3.2"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  }
}
