{
  "version": "4.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "get-stdin": "^9.0.0",
    "svgo": "^3.0.0"
  },
  "engines": {
    "node": ">=16",
    "npm": ">=7"
  }
}
