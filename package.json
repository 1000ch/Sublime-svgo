{
  "version": "4.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "get-stdin": "^9.0.0",
    "svgo": "^2.6.1"
  },
  "engines": {
    "node": ">=12.20",
    "npm": ">=7"
  }
}
